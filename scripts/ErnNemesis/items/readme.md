# Items Allowlist

To add new items to the allowlist in a patch:
 - Make a new, uniquely-named `.yaml` file in `scripts/ErnNemesis/items`.
 - The format of the yaml file should match items.yaml, with a top-level field called "items".
 - Add in record IDs to that file.
