# Actor Blocklist

To add new actors to the blocklist in a patch:
 - Make a new, uniquely-named `.yaml` file in `scripts/ErnNemesis/actors`.
 - The format of the yaml file should match actors.yaml, with a top-level field called "actors".
 - Add in record IDs to that file.
